# Лабораторна робота №7
## Тема: Computer Vision та багатопотоковість у C++

Розширення лабораторної роботи №6: додано режим детекції облич на базі нейромережі ResNet-10 SSD з асинхронним інференсом у фоновому потоці.

## Опис програми

Ця програма реалізує інтерактивну обробку відеопотоку з камери в реальному часі з використанням OpenCV.
Користувач може змінювати режими обробки зображення, взаємодіяти з відео через клавіатуру та мишу, а також змінювати параметри обробки за допомогою GUI-елементів.
Після запуску відкриється вікно з відео з камери, де можна перемикати режими та взаємодіяти з зображенням у реальному часі.

## Етапи роботи
- Захоплення відео з камери (cv::VideoCapture)
- Відображення відеопотоку у вікні
- Реалізація обробки клавіатури (перемикання режимів)
- Реалізація обробки миші (малювання прямокутників)
- Реалізація різних режимів обробки зображення
- Додавання інтерактивних елементів (trackbar)
- Виведення службової інформації (FPS)
- Організація проєкту з використанням CMake
- Реалізація багатопотокової обробки
- Інтеграція OpenCV DNN
- Детекція облич нейромережею
- Синхронізація потоків через mutex

## Реалізовані режими
1 — Grayscale (перетворення у відтінки сірого)
2 — Canny edge detection (детекція границь)
3 — Gaussian blur (розмиття)
4 — Invert colors (інверсія кольорів)
5 — Sobel filter (градієнти зображення)
6 — Glitch effect (зсув кольорових каналів)
7 — Picture-in-picture (накладання зображення)
F — Face Detection (DNN + multithreading)

## Додаткові можливості
- Zoom (W / S)
- Rotation (A / D)
- Малювання прямокутників мишею
- Очищення малюнків (R)
- Регулювання яскравості (trackbar)
- Відображення FPS
- Робота в реальному часі
- Multithreaded face detection
- Neural network inference using OpenCV DNN
- Background processing thread
- Thread-safe synchronization using mutexes

## Інструкція із запуску

### Клонування репозиторію
```bash
git clone <URL>
```

### Перехід у папку
```bash
cd lab7
```

### Встановлення залежностей
```bash
./preinstall.sh
```

### Білд
```bash
./build.sh
``` 

### Запуск
```bash
./run.sh
```

## Керування
- 1–7 — перемикання режимів
- W / S — zoom
- A / D — rotation
- R — очистити намальовані фігури
- Миша — малювання прямокутників
- F — face detection mode
- ESC — вихід

## Використані технології
- C++17
- OpenCV 4.x
- OpenCV DNN
- CMake
- std::thread
- std::mutex
- std::atomic

## Вимоги до системи
- Ubuntu 20.04 або новіше
- Наявність веб-камери
- Підтримка OpenCV з модулем DNN

## Застереження
Програма використовує веб-камеру. Якщо камера зайнята іншою програмою (Zoom, Teams тощо), відеопотік може не відкриватися.

## Особливості запуску у WSL2
Для роботи з камерою у WSL2 необхідно:

1. Встановити usbipd-win на Windows
2. Підключити камеру:
```bash
usbipd list
usbipd attach --busid <ID>
```   
3. Запустити X-сервер (GWSL або VcXsrv)

Без цих налаштувань камера у WSL2 працювати не буде.

## Структура проєкту
```bash
lab7/
├── CMakeLists.txt
├── preinstall.sh
├── build.sh
├── run.sh
├── README.md
├── assets/
│   └── logo.png
├── models/              ← створюється preinstall.sh
│   ├── deploy.prototxt
│   └── res10_300x300_ssd_iter_140000.caffemodel
├── include/
│   ├── CameraProvider.hpp
│   ├── Display.hpp
│   ├── FrameProcessor.hpp
│   ├── KeyProcessor.hpp
│   └── FaceDetector.hpp  ← нове
└── src/
    ├── CameraProvider.cpp
    ├── Display.cpp
    ├── FrameProcessor.cpp
    ├── KeyProcessor.cpp
    ├── FaceDetector.cpp  ← нове
    └── main.cpp          ← змінено
```

## Висновок

У ході виконання лабораторної роботи було розроблено інтерактивну систему обробки відеопотоку в реальному часі з використанням бібліотеки OpenCV та інструментів багатопотоковості C++.

Було опрацьовано:
- роботу з відеопотоком
- обробку подій клавіатури та миші
- реалізацію алгоритмів обробки зображень
- побудову модульної архітектури C++ застосунку
- інтеграцію готових глибоких нейромереж через модуль cv::dnn
- організацію паралельних обчислень за допомогою std::thread

Для детекції облич використовується натренована модель ResNet-10 SSD Caffe. Завдяки винесенню інференсу мережі у фоновий робочий потік (Worker Thread) та синхронізації через м'ютекси, основний потік інтерфейсу (Main Thread) залишається чутливим, забезпечуючи плавну картинку та стабільний FPS без затримок.